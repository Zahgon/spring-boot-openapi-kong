# HTTP port (matches the Spring Boot default of 8080 used by Kong/docker-compose)
quarkus.http.port=8080

# Store the generated OpenAPI schema at build time to target/openapi.json,
# so the Inso CLI can generate the Kong declarative config (kong/kong.yml) from it.
quarkus.smallrye-openapi.store-schema-directory=target
